name: main
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.0.0]
    steps:
    - name: test
      run: |
        npm install
        npm test
        ./node_modules/codecov/bin/codecov